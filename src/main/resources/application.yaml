spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/CreditConveyor
    username: postgres
    password: root

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

conveyor:
  url: http://localhost:8080
