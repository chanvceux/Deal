spring:
  datasource:
    url: jdbc:h2://mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  port: 8081

conveyor:
  url: http://localhost:8080

json:
  application: ./src/test/resources/json/Application.json
